name: SalaatiSwiftUi
options:
  bundleIdPrefix: com.salaati
  deploymentTarget:
    macOS: "13.0"
  xcodeVersion: "15.0"

settings:
  base:
    SWIFT_VERSION: "5.9"
    INFOPLIST_FILE: SalaatiSwiftUi/Info.plist

targets:
  SalaatiSwiftUi:
    type: application
    platform: macOS
    sources:
      - path: SalaatiSwiftUi
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.salaati.app
        INFOPLIST_KEY_CFBundleDisplayName: Salaati
        CODE_SIGN_STYLE: Automatic
        DEVELOPMENT_TEAM: ""
        ENABLE_PREVIEWS: YES
        GENERATE_INFOPLIST_FILE: NO
        MACOSX_DEPLOYMENT_TARGET: "13.0"
        COMBINE_HIDPI_IMAGES: YES

schemes:
  SalaatiSwiftUi:
    build:
      targets:
        SalaatiSwiftUi: all
    run:
      config: Debug
    profile:
      config: Release
    analyze:
      config: Debug
    archive:
      config: Release
